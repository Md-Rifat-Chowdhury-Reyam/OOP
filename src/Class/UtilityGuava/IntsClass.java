package Class.UtilityGuava;

//import com.google.common.primitives.Ints;
import java.util.*;

/**
 * Guava is a set of core Java libraries from Google,
 * offering a wide range of utilities that extend the capabilities of the standard Java Development Kit (JDK).
 * Ints is a utility class for primitive type int.
 * It provides Static utility methods pertaining to int primitives,
 * that are not already found in either Integer or Arrays
 */

public class IntsClass {

    public static void main(String[] args) {
        int[] arr1 = { 5, 10, 15 };
        int[] arr2 = { 20, 25 };

//        int[] arrConCat = Ints.concat(arr1, arr2);

//        System.out.println(Arrays.toString(arrConCat));
    }
}
